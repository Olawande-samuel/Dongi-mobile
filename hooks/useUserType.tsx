// import { useAuth } from "@/context/Auth";

// const useUserType = (): { userType: USERTYPE } => {
// 	const { userType } = useAuth();
// 	return { userType: (userType as USERTYPE) ?? null };
// };

// export default useUserType;
