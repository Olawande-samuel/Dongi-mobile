import { Redirect } from "expo-router";
import React from "react";

// Redirect to main index for user type selection
const Authentication = () => {
	return <Redirect href="/" />;
};

export default Authentication;
